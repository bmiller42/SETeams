import React, { Component } from 'react';
import { Text, AppRegistry, TextInput } from 'react-native';

/* export default is needed to import a class into another file */
export default class BarInput extends Component {
  render() {
    return (
      <Text>This is supposed to be a text input bar</Text>
    );
  }
}
